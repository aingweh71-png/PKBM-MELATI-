import { useState, useEffect } from "react"; import { motion } from "framer-motion"; import { Card, CardContent } from "@/components/ui/card"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Textarea } from "@/components/ui/textarea";

export default function Website() { const [form, setForm] = useState({ nama: "", nisn: "", email: "", hp: "", alamat: "", });

const [countdown, setCountdown] = useState("");

// Hitung mundur ke 5 September 2025 useEffect(() => { const targetDate = new Date("2025-09-05T00:00:00").getTime();

const interval = setInterval(() => {
  const now = new Date().getTime();
  const distance = targetDate - now;

  if (distance <= 0) {
    clearInterval(interval);
    setCountdown("Pendaftaran sudah dibuka!");
  } else {
    const days = Math.floor(distance / (1000 * 60 * 60 * 24));
    const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
    const seconds = Math.floor((distance % (1000 * 60)) / 1000);

    setCountdown(`${days}h ${hours}j ${minutes}m ${seconds}d`);
  }
}, 1000);

return () => clearInterval(interval);

}, []);

const handleChange = (e) => { setForm({ ...form, [e.target.name]: e.target.value }); };

const handleSubmit = (e) => { e.preventDefault(); alert("Formulir berhasil dikirim!"); };

return ( <div className="min-h-screen bg-gray-50 text-gray-800"> {/* Hero */} <section className="bg-blue-600 text-white py-20 text-center"> <motion.h1 initial={{ opacity: 0, y: -20 }} animate={{ opacity: 1, y: 0 }} className="text-4xl font-bold mb-4" > Pendaftaran Siswa Baru<br /> PKBM MELATI </motion.h1> <p className="text-lg">Mewujudkan pendidikan untuk semua.</p> <a href="/Brosur_PKBM_MELATI.pdf" download> <Button className="mt-6 bg-white text-blue-600 hover:bg-gray-200"> 📄 Download Brosur (PDF) </Button> </a>

{/* Countdown */}
    <div className="mt-8 text-xl font-semibold">
      ⏳ Hitung Mundur Pendaftaran: {countdown}
    </div>
  </section>

  {/* Jadwal */}
  <section className="py-16 px-6 max-w-4xl mx-auto">
    <h2 className="text-2xl font-bold mb-6 text-center">Jadwal Pendaftaran</h2>
    <ul className="space-y-3 list-disc list-inside text-lg">
      <li><strong>Pendaftaran Dibuka:</strong> 5 September 2025</li>
      <li><strong>Pendaftaran Ditutup:</strong> 1 Oktober 2025</li>
      <li><strong>Pengumuman:</strong> 5 Oktober 2025</li>
      <li><strong>Daftar Ulang:</strong> 6 - 10 Oktober 2025</li>
    </ul>
  </section>

  {/* Syarat */}
  <section className="py-16 px-6 max-w-4xl mx-auto">
    <h2 className="text-2xl font-bold mb-6 text-center">Syarat Pendaftaran</h2>
    <ul className="space-y-3 list-disc list-inside">
      <li>Fotokopi ijazah terakhir</li>
      <li>Fotokopi Kartu Keluarga (KK)</li>
      <li>Fotokopi Akte Kelahiran</li>
    </ul>
  </section>

  {/* Biaya */}
  <section className="py-16 px-6 bg-gray-100">
    <h2 className="text-2xl font-bold mb-6 text-center">Biaya & Beasiswa</h2>
    <div className="grid md:grid-cols-3 gap-6">
      <Card className="shadow-lg">
        <CardContent className="p-6">
          <h3 className="text-xl font-semibold mb-2">Pendaftaran</h3>
          <p>Gratis</p>
        </CardContent>
      </Card>
      <Card className="shadow-lg">
        <CardContent className="p-6">
          <h3 className="text-xl font-semibold mb-2">SPP</h3>
          <p>Rp100.000 / bulan</p>
          <p className="text-sm text-gray-600 mt-2">Bisa dibayar per bulan, per semester, atau per tahun</p>
        </CardContent>
      </Card>
      <Card className="shadow-lg">
        <CardContent className="p-6">
          <h3 className="text-xl font-semibold mb-2">Beasiswa</h3>
          <p>Keringanan biaya & beasiswa prestasi tersedia</p>
        </CardContent>
      </Card>
    </div>
  </section>

  {/* Form */}
  <section className="py-16 px-6 max-w-3xl mx-auto">
    <h2 className="text-2xl font-bold mb-6 text-center">Formulir Pendaftaran</h2>
    <form onSubmit={handleSubmit} className="space-y-4">
      <Input placeholder="Nama Lengkap" name="nama" value={form.nama} onChange={handleChange} required />
      <Input placeholder="NISN" name="nisn" value={form.nisn} onChange={handleChange} required />
      <Input type="email" placeholder="Email" name="email" value={form.email} onChange={handleChange} required />
      <Input placeholder="No. HP" name="hp" value={form.hp} onChange={handleChange} required />
      <Textarea placeholder="Alamat Lengkap" name="alamat" value={form.alamat} onChange={handleChange} required />
      <Button type="submit" className="w-full">Daftar</Button>
    </form>
  </section>

  {/* FAQ */}
  <section className="py-16 px-6 max-w-4xl mx-auto">
    <h2 className="text-2xl font-bold mb-6 text-center">FAQ (Pertanyaan Umum)</h2>
    <div className="space-y-4">
      <Card>
        <CardContent className="p-4">
          <h3 className="font-semibold">Apakah bisa sambil kerja?</h3>
          <p>Ya, jadwal belajar fleksibel sehingga peserta bisa tetap bekerja sambil belajar.</p>
        </CardContent>
      </Card>
      <Card>
        <CardContent className="p-4">
          <h3 className="font-semibold">Apakah ada kelas malam?</h3>
          <p>Ya, tersedia kelas malam untuk peserta yang bekerja di siang hari.</p>
        </CardContent>
      </Card>
      <Card>
        <CardContent className="p-4">
          <h3 className="font-semibold">Apakah ada beasiswa?</h3>
          <p>Ada, PKBM MELATI menyediakan beasiswa prestasi dan keringanan biaya.</p>
        </CardContent>
      </Card>
    </div>
  </section>

  {/* Kontak */}
  <section className="py-16 px-6 bg-gray-100">
    <h2 className="text-2xl font-bold mb-6 text-center">Kontak & Lokasi</h2>
    <div className="text-center space-y-2">
      <p>📍 Jalan Kota Tanjung Balai Karimun RT01 RW02</p>
      <p>📞 0857-1925-3841</p>
      <p>📧 aingweh71@gmail.com</p>
      <a href="https://wa.me/6285719253841" target="_blank" rel="noopener noreferrer">
        <Button className="mt-4">Hubungi via WhatsApp</Button>
      </a>
    </div>
    <div className="mt-8 w-full h-64">
      <iframe
        src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3989.284676216461!2d103.449!3d1.082!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x31d9895b19c9b8ff%3A0x3039d80b220cc70!2sTanjung%20Balai%20Karimun!5e0!3m2!1sid!2sid!4v1690000000000"
        width="100%"
        height="100%"
        style={{ border: 0 }}
        allowFullScreen=""
        loading="lazy"
      ></iframe>
    </div>
  </section>

  {/* Footer */}
  <footer className="bg-blue-600 text-white text-center py-6 mt-10">
    <p>© {new Date().getFullYear()} PKBM MELATI. Semua Hak Dilindungi.</p>
  </footer>
</div>

); }

